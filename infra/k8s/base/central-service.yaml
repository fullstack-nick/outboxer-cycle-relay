apiVersion: v1
kind: Service
metadata:
  name: outboxer-central
  namespace: outboxer
  labels:
    app.kubernetes.io/name: outboxer-central
    app.kubernetes.io/part-of: outboxer
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: outboxer-central
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
